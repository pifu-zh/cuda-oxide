/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated conversion interfaces: `sync` intrinsics.

use crate::conversion_interface::MirToLlvmConversion;
use crate::convert::intrinsics::{
    common::call_intrinsic, common::create_i32_const, common::inline_asm_convergent,
};
use crate::{IntrinsicBackend, context};
use dialect_nvvm::ops::{Barrier0Op, ThreadfenceBlockOp, ThreadfenceOp, ThreadfenceSystemOp};
use llvm_export::types as llvm_types;
use pliron::builtin::types::{IntegerType, Signedness};
use pliron::context::Context;
use pliron::derive::op_interface_impl;
use pliron::irbuild::dialect_conversion::{DialectConversionRewriter, OperandsInfo};
use pliron::irbuild::rewriter::Rewriter;
use pliron::op::Op;
use pliron::result::Result;

#[op_interface_impl]
impl MirToLlvmConversion for Barrier0Op {
    fn convert(
        &self,
        ctx: &mut Context,
        rewriter: &mut DialectConversionRewriter,
        _operands_info: &OperandsInfo,
    ) -> Result<()> {
        let op = self.get_operation();
        let void_ty = llvm_types::VoidType::get(ctx);
        match context::lowering_options(ctx).intrinsic_backend {
            IntrinsicBackend::LlvmNvptx => {
                let i32_ty = IntegerType::get(ctx, 32, Signedness::Signless);
                let barrier_id = create_i32_const(ctx, rewriter, 0);
                let function_ty =
                    llvm_types::FuncType::get(ctx, void_ty.into(), vec![i32_ty.into()], false);
                call_intrinsic(
                    ctx,
                    rewriter,
                    op,
                    "llvm_nvvm_barrier_cta_sync_aligned_all",
                    function_ty,
                    vec![barrier_id],
                )?;
            }
            // [PORT gfx1030 Stage3-1] `llvm.amdgcn.s.barrier()` (no operands in
            // current LLVM) is the AMDGPU workgroup barrier; the AMDGPU backend
            // cannot select the NVVM barrier intrinsics. Convergent marking is
            // handled by llvm-export's convergent-intrinsic list.
            IntrinsicBackend::Amdgcn => {
                let function_ty = llvm_types::FuncType::get(ctx, void_ty.into(), vec![], false);
                call_intrinsic(
                    ctx,
                    rewriter,
                    op,
                    "llvm_amdgcn_s_barrier",
                    function_ty,
                    vec![],
                )?;
            }
            IntrinsicBackend::LibNvvm => {
                inline_asm_convergent(
                    ctx,
                    rewriter,
                    op,
                    void_ty.into(),
                    vec![],
                    "bar.sync 0;",
                    "~{memory}",
                );
            }
        }
        rewriter.erase_operation(ctx, op);
        Ok(())
    }
}

#[op_interface_impl]
impl MirToLlvmConversion for ThreadfenceOp {
    fn convert(
        &self,
        ctx: &mut Context,
        rewriter: &mut DialectConversionRewriter,
        _operands_info: &OperandsInfo,
    ) -> Result<()> {
        let op = self.get_operation();
        let void_ty = llvm_types::VoidType::get(ctx);
        let function_ty = llvm_types::FuncType::get(ctx, void_ty.into(), vec![], false);
        call_intrinsic(
            ctx,
            rewriter,
            op,
            "llvm_nvvm_membar_gl",
            function_ty,
            vec![],
        )?;
        rewriter.erase_operation(ctx, op);
        Ok(())
    }
}

#[op_interface_impl]
impl MirToLlvmConversion for ThreadfenceBlockOp {
    fn convert(
        &self,
        ctx: &mut Context,
        rewriter: &mut DialectConversionRewriter,
        _operands_info: &OperandsInfo,
    ) -> Result<()> {
        let op = self.get_operation();
        let void_ty = llvm_types::VoidType::get(ctx);
        let function_ty = llvm_types::FuncType::get(ctx, void_ty.into(), vec![], false);
        call_intrinsic(
            ctx,
            rewriter,
            op,
            "llvm_nvvm_membar_cta",
            function_ty,
            vec![],
        )?;
        rewriter.erase_operation(ctx, op);
        Ok(())
    }
}

#[op_interface_impl]
impl MirToLlvmConversion for ThreadfenceSystemOp {
    fn convert(
        &self,
        ctx: &mut Context,
        rewriter: &mut DialectConversionRewriter,
        _operands_info: &OperandsInfo,
    ) -> Result<()> {
        let op = self.get_operation();
        let void_ty = llvm_types::VoidType::get(ctx);
        let function_ty = llvm_types::FuncType::get(ctx, void_ty.into(), vec![], false);
        call_intrinsic(
            ctx,
            rewriter,
            op,
            "llvm_nvvm_membar_sys",
            function_ty,
            vec![],
        )?;
        rewriter.erase_operation(ctx, op);
        Ok(())
    }
}
