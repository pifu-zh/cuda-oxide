[package]
name = "vecadd"
version = "0.1.0"
edition = "2024"
authors = ["Nihal Pasham <nihalp@nvidia.com>"]
license = "Apache-2.0"

# Mark as standalone crate (not part of parent workspace)
[workspace]

# This example demonstrates unified compilation with rustc-codegen-cuda.
# Build with:
#   cargo oxide run vecadd
# Or manually:
#   RUSTFLAGS="-Z codegen-backend=path/to/librustc_codegen_cuda.so" cargo build --release

[dependencies]
# Device-side intrinsics (thread indexing, barriers, etc.)
cuda-device = { path = "../../../cuda-device" }

# Host-side utilities (CudaKernel trait, etc.)
cuda-host = { path = "../../../cuda-host" }

# Host-side CUDA runtime
cuda-core = "0.3.1"

# [PORT gfx1030 Stage2] Swap the crates.io cuda-bindings (dlopen libcuda,
# build-time CUDA SDK headers) for the HIP-backed stand-in at
# ../../../host-hip/cuda-bindings: all cu* entry points translate to
# libamdhip64 at runtime. Upstream builds without this table are untouched.
[patch.crates-io]
cuda-bindings = { path = "../../../host-hip/cuda-bindings" }
